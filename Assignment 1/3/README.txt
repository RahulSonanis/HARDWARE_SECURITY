1) Extract the LIBSVM zip.You will get a LIBSVM folder. In that there is also a folder named Matlab.
2) Open MATLAB. Open the LIBSVM->Matlab folder in MATLAB.
3) compile using "make" command.
4) Just run the code using assgn command as "assgn.m" is the file name.

